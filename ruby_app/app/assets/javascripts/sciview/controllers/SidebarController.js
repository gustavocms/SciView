(function() {
    var app = angular.module('sciview')

    app.controller('SidebarController', [
        '$scope',
        '$rootScope',
        '$location',
        function($scope, $rootScope, $location) {

        }
    ]);

})();