<h2 class="ui header"><%= @chart.name %></h2>
<div class="ui segment">
  <div id="chart" class="chart with-3d-shadow with-transitions"
    data-source-url="<%= multiple_datasets_path(@chart.series) %>">
  </div>
</div>
